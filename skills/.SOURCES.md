# Vendored skill provenance

## mattpocock/skills (MIT) - installed 2026-09-07, upstream v1.2.3
Copied from https://github.com/mattpocock/skills @ main

Requested: handoff, wayfinder, teach, wizard, to-tickets
Deps pulled in (wayfinder/to-tickets call these by name):
  grilling, domain-modeling, research, prototype, setup-matt-pocock-skills

Deliberately NOT taken (overlap superpowers / code-reviewer / builtin /code-review):
  tdd, implement, code-review, grill-me, grill-with-docs, ask-matt, triage,
  to-spec, diagnosing-bugs, codebase-design, improve-codebase-architecture,
  resolving-merge-conflicts, to-questionnaire, wait-what, writing-for-agents

Update: re-copy from upstream, or switch to `/plugin install mattpocock-skills`
(plugin gives all 25 - would duplicate these 10).

## nextlevelbuilder/ui-ux-pro-max-skill (MIT) - installed 2026-09-07, upstream v2.13.0
Copied from https://github.com/nextlevelbuilder/ui-ux-pro-max-skill @ main
All 7 skills: ui-ux-pro-max, design, design-system, ui-styling, brand, slides, banner-design
(6 of 7 authored "claudekit"; only ui-ux-pro-max is nextlevelbuilder's)

LOCAL MODIFICATIONS (needed for global ~/.claude/skills install):
  - ui-ux-pro-max/SKILL.md: 11x ${CLAUDE_PLUGIN_ROOT}/.claude/skills/ -> $HOME/.claude/skills/
  - brand, design, design-system, ui-styling: relative `python3 scripts/...` /
    `node scripts/...` invocations rewritten to $HOME/.claude/skills/<skill>/scripts/...
    (74 refs total; upstream relative paths only resolve if CWD == skill dir)
  - deleted committed test artifacts: ui-styling/scripts/.coverage, tests/coverage-ui.json

Re-apply these rewrites after any upstream update, or install via CLI instead:
  npx ui-ux-pro-max-cli init --ai claude --global   (blocked by this session's classifier)
  uninstall: npx ui-ux-pro-max-cli uninstall --ai claude --global

## juliusbrussee/caveman - installed/updated 2026-09-07
Copied from https://github.com/juliusbrussee/caveman @ main (104k stars, pushed 2026-09-07)
LICENSE IS SPLIT: skills/ and evals/ are MIT. engine/ proxy/ rewriter/ browse/ mcp/
shrink/ cavemem-Go-core shared/platform/ are BSL-1.1 (NOT installed here).
BSL converts to Apache-2.0 on 2030-06-21. Self-hosted first-party use free;
offering it to third parties as hosted/managed/EMBEDDED service needs a commercial
license -> relevant if ever put inside a client deliverable. "Caveman" is a trademark.
agents/ is not explicitly classified in LICENSING.md (plain markdown, not engine-linked).

UPDATED (all 7 were stale, ~4 months old; originals in ~/.claude/.skill-backups/caveman-2026-09-07/):
  caveman, caveman-commit, caveman-review, caveman-stats, caveman-help, cavecrew, compress
  - compress: upstream dir is caveman-compress; kept local dir name `compress`
    and rewrote frontmatter name to match (dir name must equal frontmatter name)

INSTALLED NEW (13):
  standalone workflow: caveman-explore, investigate-first, lean-build, migration,
                       safe-refactor, surgical-patch, verify-and-stop
  Caveman Cloud-tied (need the hosted service): caveman-setup, caveman-discover,
                       caveman-evidence-review, caveman-optimize, caveman-manage, caveman-learn

AGENTS INSTALLED -> ~/.claude/agents/ (fixes cavecrew, which referenced them 19x but
they were absent, so every delegation failed):
  cavecrew-builder, cavecrew-investigator, cavecrew-reviewer

NOT DONE - hooks are stale, needs your call:
  ~/.claude/hooks/{caveman-activate,caveman-mode-tracker,caveman-config,caveman-stats}.js
  and caveman-statusline.sh are all from May 3 and 3-4x smaller than upstream src/hooks/.
  Upstream also adds caveman-parse.js and cavecrew-model-overrides.js, and ships
  src/hooks/install.sh + checksums.sha256. Upgrading rewrites settings.json and changes
  live session behavior, so it was left alone.

## LOCAL FIX unrelated to upstream
`design` (from ui-ux-pro-max-skill) collided with Claude Code's BUILTIN `design` skill
and silently failed to register. Renamed dir + frontmatter to `design-suite` and
repointed its 55 script path refs.

## Update pass 2026-09-07 (backups in ~/.claude/.skill-backups/update-2026-09-07/)

### obra/superpowers v6.3.0 (MIT) - REPLACED flat file with 14 real skills
Local `superpowers` was a hand-flattened 658-line single SKILL.md holding all 14
sections at once. Upstream ships them as 14 addressable skills, so the whole file
no longer loads on every invoke. Removed the flat copy; installed:
  brainstorming, dispatching-parallel-agents, executing-plans,
  finishing-a-development-branch, receiving-code-review, requesting-code-review,
  subagent-driven-development, systematic-debugging, test-driven-development,
  using-git-worktrees, using-superpowers, verification-before-completion,
  writing-plans, writing-skills
Entry point is now `using-superpowers`, not `superpowers`.
LOCAL MODS: relative script invocations made absolute -
  brainstorming/visual-companion.md   `bash scripts/start-server.sh` (1 ref)
  subagent-driven-development/*.md    `scripts/review-package` (6 refs)
  chmod +x on review-package, brainstorming/scripts/*.sh, find-polluter.sh

### mvanhorn/last30days-skill v3.3.2 -> v3.23.0 (MIT)
Was a lone SKILL.md; upstream v3.23.0 is 2.7MB with scripts/, references/, agents/.
LOCAL MODS: 7x `python3 scripts/last30days.py` -> absolute path.

### NOT UPDATABLE - no upstream to pull from
hermes-agent  local v1.0.0, upstream repo is v2026.8.31, but the local skill is a
              hand-written guide, not a copy of any upstream file. Its referenced
              installer URL still resolves (HTTP 200). Rewrite by hand if wanted.
claude-mem    local ~/.claude-mem/ is a homemade 2-script setup (capture.sh,
              summarize.sh, sessions/). NOT thedotmack/claude-mem v13.24.1 despite
              the shared name. No shared update path.
19 others     adr-writer, api-integrator, code-reviewer, eval-designer,
              frontend-design, graphify, hallucination-checker,
              ian-xiaohei-illustrations, infra-scaffolder, model-card-writer,
              pre-deployment-checklist, prompt-engineer, rag-builder,
              rapid-prototyper, system-design, technical-explainer, test-skill,
              test-writer, trace-analyzer
              -> single-file, no version/author/URL metadata anywhere in them.
              Locally authored or generated. Nothing upstream to diff against.
              If any DID come from a repo, record it here so future passes can check.
test-skill    uses lowercase `skill.md`; still registers. Placeholder, safe to delete.

## addyosmani/agent-skills v0.6.9 (MIT) - installed 2026-09-07
Copied from https://github.com/addyosmani/agent-skills @ tag 0.6.9 (92.8k stars)
All 25 skills, 4 agents, 9 commands. No vendored data, no scripts -> no path rewriting needed.

SKILLS (25): api-and-interface-design, browser-testing-with-devtools, ci-cd-and-automation,
  code-review-and-quality, code-simplification, constraint-driven-development,
  context-engineering, debugging-and-error-recovery, deprecation-and-migration,
  documentation-and-adrs, doubt-driven-development, frontend-ui-engineering,
  git-workflow-and-versioning, idea-refine, incremental-implementation, interview-me,
  observability-and-instrumentation, performance-optimization, planning-and-task-breakdown,
  security-and-hardening, shipping-and-launch, source-driven-development,
  spec-driven-development, test-driven-development, using-agent-skills
AGENTS (4) -> ~/.claude/agents/: code-reviewer, security-auditor, test-engineer,
  web-performance-auditor
COMMANDS (9) -> ~/.claude/commands/: build, code-simplify, constraints, plan, review,
  ship, spec, test, webperf
  (used .claude/commands/*.md - the native Claude Code format. Repo also has
   commands/*.toml for other agents; those were NOT installed.)

### COLLISION RESOLVED: test-driven-development
Both superpowers v6.3.0 and this repo ship a skill by that exact name.
Addy's has 9 internal cross-refs vs superpowers' 5, and superpowers' refs were all
the plugin-namespaced form `superpowers:test-driven-development`, which does not
resolve in a flat ~/.claude/skills install anyway. So:
  - addy's keeps the canonical name `test-driven-development`
  - superpowers' was renamed to `superpowers-tdd` (dir + frontmatter)
  - its 4 referencing files were repointed:
    systematic-debugging/SKILL.md, writing-skills/SKILL.md,
    writing-skills/testing-skills-with-subagents.md,
    using-superpowers/references/hermes-tools.md
  - original at ~/.claude/.skill-backups/update-2026-09-07/superpowers-test-driven-development/
Both TDD skills remain available and internally consistent.

### NOT installed - hooks/
hooks.json, sdd-cache-{pre,post}.sh, simplify-ignore.sh, session-start.sh and their
tests change tool behavior and rewrite settings.json. Left alone, same as the stale
caveman hooks. Install deliberately if wanted.

### FOUR competing methodology frameworks now coexist
superpowers (14) / mattpocock (10) / caveman workflow (7) / addyosmani (25).
All four want to own brainstorm -> plan -> implement -> review. Two meta-routers
compete at session start: `using-superpowers` and `using-agent-skills`.
This is the real cost of installing all of them - not disk.

## Leonxlnx/taste-skill (MIT) - installed 2026-09-07
Copied from https://github.com/Leonxlnx/taste-skill @ main (85.2k stars, pushed 2026-08-24)
Took the 9 Claude-compatible skills of 13. All single-file, ~225KB total.

WARNING FOR FUTURE UPDATES: in this repo the DIRECTORY name != the frontmatter `name`.
Installing by repo dir name would break registration. Mapping used:
  repo skills/taste-skill            -> design-taste-frontend      (87KB, 1206 lines)
  repo skills/brandkit               -> brandkit
  repo skills/brutalist-skill        -> industrial-brutalist-ui
  repo skills/minimalist-skill       -> minimalist-ui
  repo skills/soft-skill             -> high-end-visual-design
  repo skills/redesign-skill         -> redesign-existing-projects
  repo skills/output-skill           -> full-output-enforcement
  repo skills/imagegen-frontend-web  -> imagegen-frontend-web
  repo skills/imagegen-frontend-mobile -> imagegen-frontend-mobile

NOT INSTALLED (4) - not for Claude Code:
  gpt-tasteskill      (GPT/Codex; Python-driven randomization)
  image-to-code-skill (explicitly "for Codex")
  stitch-skill        (generates DESIGN.md for Google Stitch)
  taste-skill-v1      (deliberate legacy duplicate of taste-skill, v1 behavior only)

NOTES:
  design-taste-frontend is 1206 lines in ONE SKILL.md - it all loads on every invoke.
  It also references an "MCP image tool" / "OpenAI image tools" for image generation,
  which are not configured here.
  imagegen-frontend-{web,mobile} are image-DIRECTION skills (they write prompts/briefs);
  they need an image generator wired up to actually produce anything.
  minimalist-ui is the one that ALIGNS with the print-editorial cream+charcoal rule
  (warm monochrome, typographic contrast, no gradients, no heavy shadows).
  brandkit + high-end-visual-design + industrial-brutalist-ui push dark-tech/luxury
  aesthetics that CONFLICT with the banned-palette rules - override their defaults.

## ComposioHQ/awesome-claude-skills - installed 2026-09-07
Copied from https://github.com/ComposioHQ/awesome-claude-skills @ master
(default branch is `master`, NOT main - codeload URLs with /main 404)
74.6k stars, pushed 2026-08-10. All 28 top-level skills installed at user's request.

!! NO LICENSE FILE IN THE REPO !!
Not MIT, not Apache, nothing. Default is all-rights-reserved. Every other repo in
this file is explicitly licensed. Flagged to the user; they chose to install anyway.
Do NOT ship any of this inside a client deliverable without sorting out licensing.

MOREOVER, roughly half is vendored from anthropics/skills and is not Composio's to
relicense: artifacts-builder, canvas-design, mcp-builder, skill-creator,
webapp-testing, theme-factory, internal-comms, slack-gif-creator, brand-guidelines.
If these ever matter, replace them with copies from anthropics/skills directly.

LOCAL MODS:
  - repo dir `video-downloader` has frontmatter name `youtube-downloader`;
    installed as `youtube-downloader` so dir == name (otherwise it never registers)
  - relative script refs made absolute (19 refs across 4 skills):
    artifacts-builder(2) mcp-builder(6) youtube-downloader(8) webapp-testing(3)
  - chmod +x on bundled scripts

NOT INSTALLED:
  composio-skills/       832 auto-generated <app>-automation dirs, 3.7MB.
                         Machine-generated catalog; needs Composio's platform.
  document-skills/       nested docx/pdf/pptx/xlsx (2.7MB) - these are Anthropic's;
                         Claude Code has its own document handling.
  connect-apps-plugin/   just a plugin.json + commands/setup.md

RELEVANT TO ACTIVE PROJECTS:
  meeting-insights-analyzer -> Gavel/BoardAI      file-organizer -> SmartSorter
  invoice-organizer -> Matthew GHL PO             lead-research-assistant -> Bridgital Apollo
  content-research-writer -> Chronicle blog       tailored-resume-generator -> resume gen / job search
  langsmith-fetch -> pairs with trace-analyzer

## ayghri/i-have-adhd (MIT) - installed 2026-09-07  [PARTIAL - non-conflicting parts only]
Copied from https://github.com/ayghri/i-have-adhd @ main (27.8k stars, pushed 2026-09-01)

INSTALLED: skills/i-have-adhd/SKILL.md only (140 lines, 6.8KB). No path rewriting needed.
  Safe to coexist with caveman because it carries `disable-model-invocation: true`
  -> it NEVER auto-fires. Inert until the user types /i-have-adhd. Stays on after that
  until "stop adhd mode" / "normal mode".

DELIBERATELY NOT INSTALLED - conflicts with the existing caveman setup:
  hooks/hooks.json + hooks/always-on.{mjs,sh,ps1}
    Registers on SessionStart with matcher "startup|resume|clear|compact" - the exact
    slot ~/.claude/hooks/caveman-activate.js already occupies. Both would fire each
    session and issue contradictory output-shaping instructions. Installing this hook
    is a SWAP for caveman's, never an addition.
  skills/i-have-adhd/agents/{gemini.toml,openai.yaml}  - other-agent configs, not Claude.

RULE-LEVEL OVERLAP (left as-is; only matters while the mode is active, and activating
it is an explicit choice to use it INSTEAD of caveman):
  aligned with existing feedback memories: rule 1 (lead with next action),
    3 (one concrete next step), 4 (suppress tangents), 10 (no preamble/recap/closers)
    -> these match feedback_response_brevity + feedback_no_trailing_summary
  opposed to caveman compression: rule 5 (restate state every turn),
    6 (specific time estimates), 7 (make wins visible) all ADD text caveman strips;
    rule 2 wants numbered prose where caveman wants fragments

## FIXES 2026-09-07 (backups: ~/.claude/.skill-backups/hooks-2026-09-07/)

### 1. caveman hooks updated May-3 -> upstream current
settings.json was ALREADY wired correctly for all three (SessionStart ->
caveman-activate.js, UserPromptSubmit -> caveman-mode-tracker.js, statusLine ->
caveman-statusline.sh), so this was a pure file swap. settings.json NOT modified.
Did NOT run src/hooks/install.sh - it only re-copies files and re-checks that same
wiring, so a reviewed manual copy was equivalent and more controlled.

Replaced (all verified against upstream src/hooks/checksums.sha256, all `node --check` clean):
  caveman-activate.js       6273b -> 22061b
  caveman-mode-tracker.js   5771b -> 23241b
  caveman-config.js        10032b -> 31001b
  caveman-stats.js         14219b -> 34194b
  caveman-statusline.sh     2260b ->  4200b   (chmod +x)
Added (new upstream files, loaded as libraries by the above - NOT separate hooks,
so no new settings.json entries were needed):
  caveman-parse.js         16526b
  cavecrew-model-overrides.js 5544b
  package.json  unchanged content {"type":"commonjs"} - hooks are CommonJS, not ESM
Skipped: caveman-statusline.ps1 (Windows only), install/uninstall .sh + .ps1

Smoke-tested all three with realistic payloads: SessionStart exit 0, UserPromptSubmit
returned valid hookSpecificOutput JSON exit 0, statusline rendered [CAVEMAN] exit 0.
~/.claude/.caveman-active still reads "full" - mode state untouched.
New version adds: an `off` level, ASD-STE100 Simplified Technical English rules, and
explicit anti-patterns (don't invent abbreviations, don't add words to sound caveman,
never drop negations).
Rollback: cp ~/.claude/.skill-backups/hooks-2026-09-07/* ~/.claude/hooks/
          (settings.json.bak and .caveman-active.bak are in there too)

### 2. template-skill DELETED
Came from ComposioHQ. Was a 5-line empty stub - description literally read
"Replace with description of the skill and when Claude should use it." and the body
was "# Insert instructions below". It registered and occupied a skill slot while
carrying zero content. `skill-creator` covers authoring new skills properly.
Backed up to .skill-backups/hooks-2026-09-07/template-skill/ if ever wanted.
